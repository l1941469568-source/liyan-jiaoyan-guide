# 教研平台操作手册 PDF → 分章节 HTML 文档站 实施计划

> **执行方式**：按任务顺序执行，每个任务包含验证步骤。

**目标：** 把《湖南省数字教研工作室用户操作手册（成员）.pdf》（192 页，图文操作手册）转换为一套分章节的静态 HTML 文档站，每章一个页面，带侧边栏导航、上一页/下一页链接、截图点击放大，新手用户可直接在浏览器中阅读。

**架构：** 用 Python + pymupdf 从 PDF 中按页提取「文字块 + 截图」并按阅读顺序（坐标）排列，生成结构化 JSON；再用 Python 脚本把 JSON 渲染成静态 HTML 页面 + 共享 CSS/JS。无构建依赖、无框架，生成的站点双击 `index.html` 即可离线阅读，也可整体丢到任意静态服务器/内网部署。

**技术栈：** Python 3.11 + pymupdf（已安装）、纯 HTML/CSS/JS（无外部 CDN 依赖，保证内网/离线可用）。

---

## 已探明的 PDF 事实（执行时无需再猜）

- 文件：`C:\Users\admin\.hermes\desktop-attachments\湖南省数字教研工作室用户操作手册（成员）.pdf`
- 192 页，Word 2021 导出，**文字型 PDF**（不是扫描件，文字可直接提取，无需 OCR）
- **没有 PDF 书签**，但第 2–7 页是印刷版目录，含完整的「章节名 → 起始页码」映射（已读取，见下方附录）
- 第 1 页为封面（仅标题）；第 8–192 页为正文
- 正文每页约 100–300 字 + 2–4 张操作截图，全站截图预计 400+ 张
- 层级结构：章（一、二…十，共 10 章）→ 节（（一）（二）…，共约 48 节）→ 小节（1. 2. 3.…）
- 内容页特征：章标题页如第 8 页「一、进入与使用数字教研工作室」，节/小节标题混排在正文文字中

## 章节 → 页面映射策略

按「节」（（一）（二）…）切分页面，共约 48 个内容页 + 1 个首页。章（如「四、观课议课」跨 85 页）不单独成页，只作为侧边栏的分组节点。目录页码即为 PDF 物理页码（第 8 页 = 目录标注的 8），映射关系 1:1，无需偏移修正。

## 产出目录结构

```
C:\Users\admin\jiaoyan-guide\
├── build\                      # 构建脚本与中间产物（不进最终站点）
│   ├── extract.py              # PDF → JSON + 图片
│   ├── parse_toc.py            # 目录解析 → sections.json
│   ├── build_html.py           # JSON → HTML 页面
│   ├── content.json            # 中间产物：按页排列的文字+图片块
│   └── sections.json           # 中间产物：节 → 页码范围
└── site\                       # 最终交付物（整个文件夹即站点）
    ├── index.html              # 首页：手册简介 + 全目录
    ├── chapters\
    │   ├── 01-01-打开数字教研工作室.html
    │   ├── 01-02-申请加入工作室.html
    │   └── ...（约 48 页）
    └── assets\
        ├── css\style.css
        ├── js\main.js          # 侧边栏折叠/图片灯箱/搜索
        └── images\             # p008_1.png 等全部截图
```

---

## 任务清单

### Task 1：解析目录，生成 sections.json

**目标：** 把印刷目录（PDF 第 2–7 页）解析成结构化的「章 → 节 → 起始页码」清单。

**文件：** 创建 `C:\Users\admin\jiaoyan-guide\build\parse_toc.py`

**要点：**
- 用 pymupdf 读取第 2–7 页文本，按行正则解析三级标题：
  - 章：`^([一二三四五六七八九十]+)、\s*(.+?)\s*\.{3,}\s*(\d+)`
  - 节：`^（([一二三四五六七八九十]+)）\s*(.+?)\s*\.{3,}\s*(\d+)`
  - 小节：`^(\d+)\.\s*(.+?)\s*\.{3,}\s*(\d+)`
- 输出 `sections.json`：每节含 `{id: "01-02", chapter: "一、进入与使用数字教研工作室", title: "申请加入工作室", start_page: 9, end_page: 10}`（end_page = 下一节 start_page − 1，最后一节为 192）
- 注意目录行尾部有全角空格和点线，正则需兼容；「十、查看积分」无小节，直接到 191 页

**验证：** 运行 `python parse_toc.py`，打印解析结果：
- 章节数 = 10，节总数 ≈ 48
- 所有节的 start_page 单调递增，最大 end_page = 192
- 抽查：「（二）申请加入工作室」start_page = 9；「（一）发布公开课」start_page = 53

---

### Task 2：按页提取文字块与截图，生成 content.json

**目标：** 从第 8–192 页逐页提取文字与图片，按版面坐标排序，保证「文→图→文→图」的阅读顺序与原文一致。

**文件：** 创建 `C:\Users\admin\jiaoyan-guide\build\extract.py`

**要点：**
- `page.get_text("dict")` 取文字块（含 bbox）；`page.get_image_info(xrefs=True)` 取图片（含 bbox 和 xref）
- 每页把所有文字块 + 图片块按 `(bbox.y0, bbox.x0)` 排序，输出统一块列表：`{"type": "text"|"image", "text"/"src", "bbox"}`
- 图片用 `doc.extract_image(xref)` 存为 `site\assets\images\p{页码:03d}_{序号}.{ext}`（保留原始 png/jpeg，不转码）
- 同一 xref 在多页复用时去重（记录已导出的 xref 集合）
- 过滤页眉页脚：丢弃 bbox 在页面顶部/底部边缘且内容为页码/手册名的块（先用 5 页抽样确认有无页眉页脚，没有则跳过此步）
- 输出 `content.json`：`{"8": [blocks...], "9": [...], ...}`

**验证：** 运行后检查：
- `site\assets\images\` 中图片总数 ≥ 350（预期 400 左右）
- `content.json` 中第 8 页第一块文字含「一、进入与使用数字教研工作室」
- 随机抽 3 张图片用浏览器打开确认清晰可读

---

### Task 3：设计站点样式与布局（style.css）

**目标：** 文档站三件套：左侧章节目录树 + 右侧内容区 + 顶部面包屑，风格清爽、面向新手教师（大字号、宽行距、截图加边框阴影）。

**文件：** 创建 `C:\Users\admin\jiaoyan-guide\site\assets\css\style.css`

**要点：**
- 布局：左侧固定 280px 侧边栏（章可折叠，节为链接，当前节高亮），右侧 max-width 860px 内容区
- 中文阅读优化：正文 16–17px、行高 1.8、微软雅黑/苹方字体栈
- 截图样式：`max-width:100%; border:1px solid #ddd; border-radius:6px; box-shadow`；`cursor:zoom-in`；`loading="lazy"`
- 操作步骤文字中的「注意：」段落渲染为黄色提示块（blockquote.callout）
- 上一页/下一页按钮：内容区底部左右分布
- 响应式：≤768px 时侧边栏变抽屉（汉堡按钮开合）
- 打印样式：`@media print` 隐藏侧边栏，方便需要纸质版的用户
- 不引用任何外部 CDN/字体，全部内联或本地

**验证：** 用一个手写 demo HTML 引用该 CSS，在浏览器打开确认布局、字体、图片样式正常。

---

### Task 4：编写 main.js（交互）

**文件：** 创建 `C:\Users\admin\jiaoyan-guide\site\assets\js\main.js`

**要点：**
- 图片灯箱：点击截图 → 全屏遮罩显示原图，ESC/点击关闭（约 40 行原生 JS）
- 侧边栏：当前章节自动展开，其他章折叠；移动端汉堡开合
- 简单站内搜索（可选但建议做）：构建时生成 `assets/js/search-index.json`（节标题 + 正文纯文本），JS 前端过滤跳转，无需服务端

**验证：** demo 页面上点击截图能放大；折叠/展开正常；搜索「直播」能列出相关节。

---

### Task 5：生成 HTML 页面（build_html.py）

**目标：** 用 content.json + sections.json 渲染全部页面。

**文件：** 创建 `C:\Users\admin\jiaoyan-guide\build\build_html.py`

**要点：**
- 页面模板（Python 字符串模板即可，不引模板引擎）：
  - `<head>` 引用 `../assets/css/style.css`、defer 引用 `../assets/js/main.js`，`<title>` = 节标题
  - 侧边栏：完整章/节树（每页都渲染同一棵树，当前页加 `.active`）
  - 内容区：按节把 start_page–end_page 的块渲染为 `<p>` / `<figure><img><figcaption>`；识别「N. 小节标题」模式渲染为 `<h3 id="...">`，章/节标题为 `<h1>/<h2>`
  - 「注意」「提示」开头的段落包成 `<div class="callout">`
  - 底部 `← 上一节：标题` / `下一节：标题 →`（首节无上一节，末节无下一节）
- 小节标题清洗：正文里的「3.上传教案/学习任务单 」可能缺空格、带尾部空格，用 `^\s*(\d{1,2})\s*[.、．]\s*(.+?)\s*$` 匹配并与 sections.json 小节名比对确认
- 生成 `index.html`：手册封面信息 + 使用说明（3–5 句新手指引）+ 完整目录卡片
- 文件名：`{章:02d}-{节:02d}-{标题拼音或简化中文}.html`，中文文件名在 Windows/浏览器均可用；若担心服务器兼容性则用纯编号 `01-02.html`（推荐，最稳）

**验证：**
- 生成文件数 = 节数 + 1（index）
- 抽查 3 个页面：浏览器打开，确认侧边栏高亮正确、图片全部显示、上一页/下一页链接互相衔接
- 全站 grep 检查无 `src=""` 或引用不存在的图片（脚本里做断言：每个引用的图片文件存在）

---

### Task 6：整站质量检查与交付

**步骤：**
1. 图片完整性：统计 HTML 中 `<img>` 引用数 vs `assets\images\` 文件数，输出报告（允许未引用的冗余图，不允许缺失）
2. 链接完整性：脚本遍历所有 `<a href>` 本地链接，确认目标文件存在
3. 用 `open_preview` 或浏览器打开 `site\index.html`，人工走查 3 条典型路径：
   - 新手路径：首页 → 一、（二）申请加入工作室 → 下一节
   - 最长章路径：四、观课议课 →（一）发布公开课 → 发布直播课小节
   - 手机端宽度（开发者工具 375px）确认抽屉导航可用
4. 与原 PDF 对照抽查 2 节，确认无文字遗漏、无图片错位
5. 向用户交付 `site\` 文件夹路径 + MEDIA 预览

**验证：** 上述 1–4 全部通过。

---

## 文件清单（会创建的文件）

| 文件 | 作用 |
|---|---|
| `jiaoyan-guide\build\parse_toc.py` | 目录解析 |
| `jiaoyan-guide\build\extract.py` | 文字/图片提取 |
| `jiaoyan-guide\build\build_html.py` | HTML 生成 |
| `jiaoyan-guide\site\assets\css\style.css` | 全站样式 |
| `jiaoyan-guide\site\assets\js\main.js` | 交互 |
| `jiaoyan-guide\site\index.html` + `chapters\*.html` | 最终页面（约 49 个） |
| `jiaoyan-guide\site\assets\images\*` | 全部截图（400 张左右） |

## 风险与权衡

- **图文顺序错乱风险（中）：** Word 导出的 PDF 块顺序通常可靠，但个别页图片可能是浮动排版。对策：Task 2 按坐标排序 + Task 6 人工对照抽查 2 节。
- **小节标题识别遗漏（中）：** 若某些小节标题与正文同字号，正则可能漏判。对策：以 sections.json 的小节清单为准做比对，缺失时告警人工补。
- **图片体积：** 400 张截图全量可能 100MB+，但对本地/内网分发可接受；不压缩以保清晰度（教师要看清按钮上的小字）。如后续要放公网，可再加 WebP 压缩任务。
- **待确认（不阻塞，按默认值执行）：** 页面文件名用纯编号 `01-02.html`（兼容性最好）；站点标题用「数字教研工作室用户操作手册」。

## 附录：章节目录（从 PDF 第 2–7 页提取）

```
一、进入与使用数字教研工作室 (p8)：打开/申请加入/进入工作空间/退出
二、课程资源 (p12)：浏览/备课本管理(8小节)/上传成套课程资源(17小节)/协同备课(6小节)
三、专题资源 (p51)：浏览/上传/编辑/查看审核/删除
四、观课议课 (p53)：发布公开课(8小节)/观课议课(16小节)/听课本(6小节)/听课任务(2小节)
五、集体教研 (p145)：查找/发起/修改/主备/参与/转让/退出/分享/审阅/导出/删除/工作要求
六、线上竞赛 (p158)：查看/报名/提交作品/查看结果/评委评审
七、开放日活动 (p169)：查看/参与
八、话题讨论 (p176)：发布/编辑删除/查看参与
九、培训研修 (p180)：查看/报名/课程学习/线上研修/研修日志/培训作业/交流讨论/培训记录
十、查看积分 (p191)
```
